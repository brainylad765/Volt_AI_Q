import 'package:flutter/material.dart';

class EquivalenceCard extends StatelessWidget {
  const EquivalenceCard({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: Implement equivalence card widget
    return const SizedBox();
  }
}
